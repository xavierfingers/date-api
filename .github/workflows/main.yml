name: CI
on:
  - workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: npm install express && node main.js >> outs.txt && curl -T ntfy.sh -X POST ntfy.sh/mytopic